// declare a variable
let Weekday = true;

// Print the value
console.log("Is today a weekday?", Weekday);

// Changing to opposite value
Weekday = !Weekday;

// Print the new value
console.log("Is today a weekday?", Weekday);